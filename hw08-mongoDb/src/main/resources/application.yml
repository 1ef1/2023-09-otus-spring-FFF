spring:
  data:
    mongodb:
      authentication-database: admin
      username: root
      password: root
      database: user_db
      port: 27017
      host: localhost
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none